{
  "type": "module",
  "dependencies": {
    "canvas": "^3.1.0",
    "lil-gui": "^0.20.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.1.0"
  }
}
